import styled from "styled-components";

const StyledHeader = styled.header`
    
`

const Usecontext_st = () => {
    return(
        <StyledHeader>This is Header</StyledHeader>
    )
}

export default Usecontext_st;